import 'package:flutter/material.dart';

class UserIndex extends StatelessWidget {
  const UserIndex({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('UserIndex');
  }
}
