import 'package:flutter/material.dart';

class DailyIndex extends StatelessWidget {
  const DailyIndex({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('DailyIndex');
  }
}
