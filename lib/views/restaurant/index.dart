import 'package:flutter/material.dart';

class RestaurantIndex extends StatelessWidget {
  const RestaurantIndex({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('RestaurantIndex');
  }
}
